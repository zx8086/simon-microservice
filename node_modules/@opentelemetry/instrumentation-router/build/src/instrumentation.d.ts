import { InstrumentationBase, InstrumentationNodeModuleDefinition } from '@opentelemetry/instrumentation';
import type * as Router from 'router';
export default class RouterInstrumentation extends InstrumentationBase<typeof Router> {
    constructor();
    private _moduleVersion?;
    init(): InstrumentationNodeModuleDefinition<Router.RouterConstructor>;
    private _requestHandlerPatcher;
    private _errorHandlerPatcher;
    private _setupSpan;
}
//# sourceMappingURL=instrumentation.d.ts.map